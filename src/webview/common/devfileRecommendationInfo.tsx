import { Alert } from "@mui/material";
import * as React from "react";


export function DevfileRecommendationInfo() {
    return (
        <Alert severity='info'>
            Recommended based on a scan of langauges and structure the project.
        </Alert>
    );
}
